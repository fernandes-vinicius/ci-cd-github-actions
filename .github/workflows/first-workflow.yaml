name: first-workflow
run-name: Primeiro workflow

on:
    - push

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: Hello World
              run: echoo "Hello World"
